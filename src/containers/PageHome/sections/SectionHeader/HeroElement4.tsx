import { Avatar, styled } from '@mui/material'

const HeroElement4 = styled(Avatar)(() => ({
  position: 'absolute',
  bottom: '10%',
  right: 0,
  width: 'calc(100% * 5 / 6)',
  height: 'auto',
  zIndex: -1,
}))

export default HeroElement4
