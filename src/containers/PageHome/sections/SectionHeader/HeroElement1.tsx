import { Avatar, styled } from '@mui/material'

const HeroElement1 = styled(Avatar)(() => ({
  position: 'absolute',
  bottom: '27%',
  left: 10,
  width: '13%',
  height: 'auto',
}))

export default HeroElement1
