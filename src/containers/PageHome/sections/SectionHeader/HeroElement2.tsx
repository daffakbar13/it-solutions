import { Avatar, styled } from '@mui/material'

const HeroElement2 = styled(Avatar)(() => ({
  position: 'absolute',
  bottom: '29.3%',
  left: 'calc(50% - 14%)',
  width: '14%',
  height: 'auto',
}))

export default HeroElement2
