import { Avatar, styled } from '@mui/material'

const HeroElement3 = styled(Avatar)(() => ({
  position: 'absolute',
  top: 0,
  right: 'calc(-72px + 5%)',
  width: '144px',
  height: '144px',
  zIndex: -1,
}))

export default HeroElement3
